# Manager (admin-only header for prototype)
ADMIN_TOKEN=dev-admin-token

# Optional override:
# JWT_SECRET=change-me

# RabbitMQ edge-agent user password (change in production)
RABBITMQ_EDGE_PASSWORD=edge-secure-password-change-me
